import facebook
import requests

# graph = facebook.GraphAPI('USERACCESSTOKEN')
# app_id = '' # Obtained from https://developers.facebook.com/
# app_secret = '' # Obtained from https://developers.facebook.com/

# # Extend the expiration time of a valid OAuth access token.
# extended_token = graph.extend_access_token(app_id, app_secret)
# print (extended_token) #verify that it expires in 60 days

